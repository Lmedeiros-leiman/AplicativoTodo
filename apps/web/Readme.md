

Frontend está todo quebrado.

primeiro que o Layoult tem UI "bonita" mas é obivamente AI slop.

segundo que a UX ta horrivel e básicamente não houve pensamento aprofundado. 

o backend do armazenamento local web tambem está uma "bagunça", mas pelo menos este até faz sentido pois eu pedi para priorizar o ID sequencia (que é gerado ao armazenar o objeto em qualquer infra, mantendo organizado pelo ID referencia), boa sorte buscar por ID referencia no front de forma estática.

mas OK já que esse projeto buscava entender a utilização de DDD em um projeto full-stack em um monorepo.

tambem detalhar que nem olhei o código, mas já consigo ver que a IA só fez funcionar. pela quantidade de arquivo duvido existir qualquer tipo de lógica de componente ou algo pra ajudar com gerenciamento de estado.